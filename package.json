{
  "name": "buckshot-mp",
  "version": "1.0.0",
  "description": "Multiplayer-Web-Game based on Buckshot Roulette",
  "main": "index.js",
  "scripts": {
    "testUpload": "jest --clearCache && jest --detectOpenHandles --forceExit --collectCoverage --collectCoverageFrom=classes/**/*.{js,jsx} --reporters=jest-junit",
    "test": "jest --clearCache && jest --detectOpenHandles --forceExit --collectCoverage",
    "start": "node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  }
}
